{
  "name": "@sagua/worker",
  "type": "module",
  "scripts": {
    "format": "rome format . --write ",
    "test": "jest --passWithNoTests",
    "clean": "del-cli ./dist",
    "build": "swc ./src -d ./dist -q",
    "watch": "chokidar \"./src/**/*\" -c \"pnpm build\" --silent --initial",
    "start:prod": "node ./dist/index.js"
  },
  "dependencies": {
    "@sagua/core": "workspace:*"
  },
  "devDependencies": {
    "@jest/globals": "^29.6.2",
    "@types/node": "^20.5.0"
  }
}
